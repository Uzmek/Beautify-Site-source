# Approved Results composition

Implemented from the UI image generated in this conversation and selected by the user on 2026-09-06. The sole requested copy change is the main action: **Nouvelle analyse**, retaining its existing `sparkles` icon and `ANA-01` destination.

`dist/assets/results-composition-v1.webp` is a lossless web export of the approved 852 × 1847 image. Four CSS image windows reuse its original artwork without regenerating it. Their source rectangles are 340 × 304 pixels at (60, 435), (452, 435), (60, 952), and (452, 952). The windows exclude the image's labels, buttons, and navigation. All visible labels, controls, framing, and navigation are native HTML/CSS/SVG and remain interactive.

An empty profile displays the approved short introduction. Existing reports retain their destinations and receive a small availability check; saved simulations retain their count and collection. The copper action uses the existing navigation, photo, analysis and paywall flow.
